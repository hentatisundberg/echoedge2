python3.12 -m venv .venv_echoregions
source .venv_echoregions/bin/activate
python -m pip install --upgrade pip

python -m pip install --upgrade --no-cache-dir -r requirements.txt
#python -m pip install -e . # install library for this repo
